source "https://rubygems.org"

# This gem brings in Jekyll and the GitHub Pages approved dependency versions
gem "github-pages", group: :jekyll_plugins

# To be able to run `bundle exec jekyll serve` locally 